[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "probeagent-ai"
version = "0.3.5"
description = "Offensive security testing for AI agents"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [{ name = "Suma Movva" }]
keywords = ["security", "ai", "agents", "red-teaming", "pentesting"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
]
dependencies = [
    "typer>=0.9",
    "rich>=13.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff==0.15.21",  # pinned: unpinned ruff drifts and breaks CI when new rules land
    "respx>=0.21",
    "pre-commit>=3.0",
]
game = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "websockets>=12.0",
]
pyrit = [
    "pyrit>=0.14,<0.16",
]
demo = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "anthropic>=0.34",
]

[project.urls]
Homepage = "https://github.com/sumamovva/probeagent"
Repository = "https://github.com/sumamovva/probeagent"
Issues = "https://github.com/sumamovva/probeagent/issues"
Changelog = "https://github.com/sumamovva/probeagent/blob/main/CHANGELOG.md"

[project.scripts]
probeagent = "probeagent.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
probeagent = ["web/*.html", "profiles/*.yaml"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100
